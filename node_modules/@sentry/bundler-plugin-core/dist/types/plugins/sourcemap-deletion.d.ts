import { Hub, NodeClient } from "@sentry/node";
import { UnpluginOptions } from "unplugin";
import { Logger } from "../sentry/logger";
interface FileDeletionPlugin {
    handleRecoverableError: (error: unknown) => void;
    waitUntilSourcemapFileDependenciesAreFreed: () => Promise<void>;
    sentryHub: Hub;
    sentryClient: NodeClient;
    filesToDeleteAfterUpload: string | string[] | undefined;
    logger: Logger;
}
export declare function fileDeletionPlugin({ handleRecoverableError, sentryHub, sentryClient, filesToDeleteAfterUpload, waitUntilSourcemapFileDependenciesAreFreed, logger, }: FileDeletionPlugin): UnpluginOptions;
export {};
//# sourceMappingURL=sourcemap-deletion.d.ts.map